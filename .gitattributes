# Auto detect text files and perform LF normalization
* text=auto
*.pth filter=lfs diff=lfs merge=lfs -text

# Excluding the usage of abnormal languages ​​when using jupyter notebook
*.ipynb linguist-generated=true
